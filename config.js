/**
 * Open in Freedium - configuration.
 *
 * Change `baseUrl` below to point the extension at a different Freedium
 * instance or mirror. This is the only place you need to edit the domain.
 * A trailing slash is optional - it is normalized automatically.
 *
 * Loaded before content.js (see manifest.json), so content.js reads this value.
 */
window.OpenInFreediumConfig = {
  baseUrl: "https://freedium-mirror.cfd/",
};
